pipeline {
    agent {
       label 'gcloud-build--rocky-linux-8--x64'
    }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn clean package javadoc:javadoc' 
            }
        }
    }
}
